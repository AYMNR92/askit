{
  "name": "ai-support-saas",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run widget\" \"npm run dashboard\"",
    "backend": "backend\\venv\\Scripts\\python -m uvicorn app.main:app --reload --app-dir backend",
    "widget": "npm run dev --prefix widget-embed",
    "dashboard": "npm run dev --prefix frontend-dashboard"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}